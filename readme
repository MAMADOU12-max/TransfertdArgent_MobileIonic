Mise en place 

For run this project, you must have Ionic on your computer  => sudo npm install -g @ionic/cli

1/ If all is ok, we can clone project now :: git clone https://github.com/MAMADOU12-max/TransfertdArgent_MobileIonic.git

2/ Install all dependance::  npm install
